from setuptools import setup, find_packages

setup(
    name='Shopify',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    author='Vignesh',
    description='A shopify package',
)